<Window x:Class="Pixellation.Components.Dialogs.AboutDialog.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resource="clr-namespace:Pixellation.Properties"
        mc:Ignorable="d" KeyDown="Window_KeyDown" WindowStartupLocation="CenterScreen"
        Title="{Binding Source={x:Static resource:Settings.Default}, Path=AboutDialogTitle}" Height="170" Width="525">
    <StackPanel>
        <Label Content="{Binding Source={x:Static resource:Settings.Default}, Path=AboutTitle}"></Label>
        <GroupBox Header="{Binding Source={x:Static resource:Settings.Default}, Path=AboutCreditsHeader}">
            <Label Content="{Binding Source={x:Static resource:Settings.Default}, Path=AboutCreditsAuthor}"></Label>
        </GroupBox>
        <GroupBox Header="{Binding Source={x:Static resource:Settings.Default}, Path=AboutLicenseHeader}">
            <Label Content="{Binding Source={x:Static resource:Settings.Default}, Path=AboutLicenseContent}"></Label>
        </GroupBox>
    </StackPanel>
</Window>